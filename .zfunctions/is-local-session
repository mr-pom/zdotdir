#!/bin/zsh
# True when NOT in a remote SSH session (safe to start a local ssh-agent).
# On a remote host the agent is usually forwarded, so we must not override it.
[[ -z "$SSH_CONNECTION" && -z "$SSH_CLIENT" && -z "$SSH_TTY" ]]
